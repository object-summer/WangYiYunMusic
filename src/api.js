import Promise from 'promise'
import axios from 'axios'
import store from '../store'
import {Message} from 'element-ui'

axios.defaults.withCredentials = true
let Api = {}
Api.login = function (loginName, password, autoLogin) {
  
}
